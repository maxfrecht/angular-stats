import { BordureDirective } from './bordure.directive';

describe('BordureDirective', () => {
  it('should create an instance', () => {
    const directive = new BordureDirective();
    expect(directive).toBeTruthy();
  });
});
